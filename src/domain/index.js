const { Company, CompanyGenerator } = require('./Company');
const { Discipline, DisciplineGenerator } = require('./Discipline');
const { Iniciative, IniciativeGenerator } = require('./Iniciative');
const { Patent, PatentGenerator } = require('./Patent');
const { PDI, PDIGenerator } = require('./PDI');
const { Skill, SkillGenerator } = require('./Skill');

module.exports = {
  Company,
  CompanyGenerator,
  Discipline,
  DisciplineGenerator,
  Iniciative,
  IniciativeGenerator,
  Patent,
  PatentGenerator,
  PDI,
  PDIGenerator,
  Skill,
  SkillGenerator,
}
